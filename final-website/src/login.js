function checkLogin(event){
    var user = document.getElementById('username');
    var pass = document.getElementById('password');

    if(user != "" & pass != ""){
        // send api call to check if legal user/pass in database
    }else{
        //Add error child to login page.
    }

}